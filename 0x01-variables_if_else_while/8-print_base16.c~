#include <stdio.h>
/**
 *main - Print base 16
 *
 *Return: 0
 */
int main(void)
{
char a = '0';
while (a <= '9')
{
putchar(a);
a++;
}
char bw = 'a';
while (bw <= 'f')
{
putchar(a);
a++;
}
putchar('\n');
return (0);
}
